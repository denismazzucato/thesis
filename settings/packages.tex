\newif\ifdraft
%%%%\drafttrue % Default is draft mode; comment this line for final version

% Conditional inclusion of showkeys with appropriate option
\ifdraft
  \usepackage[final]{showkeys}
\else
  \usepackage[final]{showkeys}
\fi

\usepackage{ifthen}
\usepackage{etoolbox}

\usepackage{suffix} %% for appending suffixes to commands

\usepackage{xargs} %% for newcommandx
\usepackage{xparse}

\usepackage{scalerel} %% for smaller fonts when subscripts
\usepackage{galois} %% for galois connections

\usepackage{multirow} % For multirow tables
\usepackage{floatflt}% For wrapping figures around text
\usepackage{booktabs} %% for toprule, midrule, bottomrule in tables
\usepackage{makecell} %% easy newline inside tables
\usepackage{subcaption} %% for subfigures
\usepackage{multirow} %% merge cells verticall

\usepackage{tikz}
\usetikzlibrary{decorations.pathreplacing,calligraphy} %% for curly brackets in graphs
\usetikzlibrary{calc}
\usepackage{pgfplots}
\pgfplotsset{compat=newest}
\usepackage{pgfplotstable}

\usepackage[shortcuts]{extdash} %% for non-breaking hyphens


\usepackage{makecell} %% easy newline inside tables
\usepackage{multirow} %% merge cells vertically
\usepackage{booktabs} %% for toprule, midrule, bottomrule in tables
\usepackage{adjustbox} %% for adjusting table width
